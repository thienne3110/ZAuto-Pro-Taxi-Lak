name: Build ZAuto VIP Production

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    env:
      PYTHON_VERSION: "3.11"
      JAVA_VERSION: "17"

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      - name: ⚡ Cache Buildozer & Android SDK/NDK
        uses: actions/cache@v3
        with:
          path: |
            ~/.buildozer
            .buildozer
          key: buildozer-v2-clean-${{ runner.os }}-${{ hashFiles('buildozer.spec') }}
          restore-keys: |
            buildozer-v2-clean-${{ runner.os }}-

      - name: ⚙️ Install System Dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            zip unzip git autoconf automake libtool \
            pkg-config zlib1g-dev libncurses5-dev \
            libffi-dev libssl-dev ccache libjpeg-dev \
            libpng-dev ffmpeg libsdl2-dev \
            libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev

      - name: 📦 Install Python Packages
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install Cython==0.29.36 buildozer==1.5.0

      - name: 📜 Accept Android Licenses
        run: |
          mkdir -p ~/.android
          touch ~/.android/repositories.cfg

      - name: 🛡️ Fail-Fast (Kiểm dịch cấu trúc cơ bản)
        run: |
          echo "Đang quét lỗi cấu trúc và file..."
          find . -type f -name "*.py" -not -path "*/\.buildozer/*" -not -path "*/bin/*" -print -exec python -m py_compile {} \;
          FILES_TO_CHECK=("java/org/zauto/ZaloForegroundService.java" "java/org/zauto/ZaloWebManager.java" "res/xml/accessibility_config.xml" "buildozer.spec")
          for FILE in "${FILES_TO_CHECK[@]}"; do
            if [ ! -f "$FILE" ]; then echo "::error::THIẾU FILE BẮT BUỘC: $FILE"; exit 1; fi
          done

      # -------------------------------------------------------------
      # 🧹 BƯỚC DỌN DẸP CACHE TẬN GỐC THEO CHỈ ĐỊNH CỦA BẠN
      # -------------------------------------------------------------
      - name: 🧹 Clean Build Cache (Xóa rác Pillow cũ)
        run: |
          echo "Đang xóa bộ nhớ đệm Buildozer cũ chứa Pillow lỗi..."
          rm -rf .buildozer
          rm -rf ~/.buildozer
          rm -rf bin
          echo "✅ Đã dọn dẹp sạch sẽ!"

      # -------------------------------------------------------------
      # 🔨 LỆNH BUILD CHÍNH THỨC
      # -------------------------------------------------------------
      - name: 🔨 Build Debug APK
        run: |
          buildozer android clean
          buildozer -v android debug 2>&1 | tee build.log

      - name: 🚨 Quét và Phiên dịch lỗi tiếng Việt
        if: failure()
        run: |
          grep -i -E "error|exception|traceback|failed|conflict|not resolved|syntax|no matching distribution" build.log > error_summary.txt || true
          
          echo "### 🚨 BÁO CÁO LỖI" >> $GITHUB_STEP_SUMMARY
          echo "Hệ thống biên dịch đã bị sập. Dưới đây là phân tích nguyên nhân:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          while IFS= read -r line; do
            if [[ "$line" == *"No matching distribution"* ]]; then
               echo "- ❌ **LỖI TẢI THƯ VIỆN:** Không tìm thấy thư viện. (Hãy kiểm tra lại Pillow 10.0.1 hoặc lùi về 9.5.0)" >> $GITHUB_STEP_SUMMARY
               echo "   - *Log gốc:* \`$line\`" >> $GITHUB_STEP_SUMMARY
            else
               echo "- ❌ **LỖI KHÁC:** \`$line\`" >> $GITHUB_STEP_SUMMARY
            fi
          done < error_summary.txt
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "<details><summary><b>🗂️ BẤM VÀO ĐÂY ĐỂ XEM TOÀN BỘ LOG LỖI</b></summary>" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`text" >> $GITHUB_STEP_SUMMARY
          cat error_summary.txt >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "</details>" >> $GITHUB_STEP_SUMMARY

          echo "::error::Build thất bại! Hãy vào mục Summary để xem nguyên nhân."
          exit 1

      - name: 🚀 Upload APK
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: ZAuto_VIP_Final_APK
          path: bin/*.apk
